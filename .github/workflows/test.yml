name: Test
on:
  push:
  schedule:
    - cron: '0 0 1 * *'  # monthly
jobs:
  linux:
    runs-on: ubuntu-latest
    steps:
      - run: sudo apt-get install autopoint libgtk2.0-dev libgsl-dev
      - uses: actions/checkout@v3
      - run: ./bootstrap.sh && ./configure && make
